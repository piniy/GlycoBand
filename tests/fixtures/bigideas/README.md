# BIG IDEAs Test Fixtures

Tests generate small timestamped BVP and Dexcom EGV files at runtime. Every fixture must retain an
explicit three-digit participant ID and portable BVP/CGM source paths. Labels may use only CGM at or
before the endpoint. Window counts and participant-support counts must remain separate.

Fixtures are software tests only and are not biological evidence.

